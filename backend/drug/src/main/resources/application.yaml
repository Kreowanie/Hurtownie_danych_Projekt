server:
  port: 8088

spring:
  application:
    name: drug

  datasource:
    url: 'jdbc:sqlserver://Kreo\SQLEXPRESS;databaseName=HD_04;encrypt=true;trustServerCertificate=true'
    username: test01
    password: test01
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true